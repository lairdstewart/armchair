# Import secrets file (not committed to git)
spring.config.import=optional:classpath:application-secret.properties

# Local H2 Configuration (for local development)
# spring.datasource.url=jdbc:h2:mem:bookdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Automatically create new database tables and/or columns based on Java entities
spring.jpa.hibernate.ddl-auto=update

# Google OAuth2 Configuration
# Client ID and secret are in application-secret.properties (not committed to git)
spring.security.oauth2.client.registration.google.scope=openid

# Google Books API Configuration
# API key is in application-secret.properties (not committed to git)
