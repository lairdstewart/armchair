Currently secrets are stored in a file called template secrets.TXT this works
well for a single work tree but I'm looking to work with multiple work trees to
do parallel cloud code instances now therefore I'm not gonna be able to copy
and paste that secret properties file every time a new work tree created cause
I'd like the Claude code instances to create their own work tree themselves
therefore I need a better way to store my secrets these are like my API keys in
my Google developer keys therefore I'd like to store them in environment
variables I believe this is possible I think you can also maybe store them in a
.env directory but I think that also wouldn't work with work trees because
then I would need a new .env for every work tree so I hope they store them in
environment variables do a little thinking and let me know what I can do to
solve this problem and then make a plan to execute it if it is something like
environment variables and that's not writing any code just tell me which
environment variables I need to create and update the code to use those instead
of using the properties well thanks


